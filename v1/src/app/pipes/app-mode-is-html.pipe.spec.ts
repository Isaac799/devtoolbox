import { AppModeIsHtmlPipe } from './app-mode-is-html.pipe';

describe('AppModeIsHtmlPipe', () => {
  it('create an instance', () => {
    const pipe = new AppModeIsHtmlPipe();
    expect(pipe).toBeTruthy();
  });
});
