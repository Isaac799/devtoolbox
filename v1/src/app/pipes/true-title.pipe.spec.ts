import { TrueTitlePipe } from './true-title.pipe';

describe('TrueTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new TrueTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
