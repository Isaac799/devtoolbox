import { AppModeCanPreviewPipe } from './app-mode-can-preview.pipe';

describe('AppModeCanPreviewPipe', () => {
  it('create an instance', () => {
    const pipe = new AppModeCanPreviewPipe();
    expect(pipe).toBeTruthy();
  });
});
