import { PrettyTypePipe } from './pretty-type.pipe';

describe('PrettyTypePipe', () => {
  it('create an instance', () => {
    const pipe = new PrettyTypePipe();
    expect(pipe).toBeTruthy();
  });
});
