package {{ renderCamel .Name }}

import (
	"database/sql"
	"encoding/json"
	"errors"
	"fmt"
	"io"
	"net/http"
	"strconv"
	"time"
)

// errors related to http handling
var (
	ErrBadParam = errors.New("unacceptable parameter")
)

// NewErrBadParam provides a formatted error for a poor param
func NewErrBadParam(s string) error {
	return fmt.Errorf("%w: %s", ErrBadParam, s)
}

{{- range $index, $element := .Entities }}
{{ template "handler" $element }}
{{- end }}