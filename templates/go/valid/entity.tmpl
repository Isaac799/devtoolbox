{{- define "entity" }}
func ({{ renderCamel .Name }} *{{ renderPascal .Name }}) Valid() error {
    const label = "{{ .Name }}"
{{- range $index, $element := .NonPrimary }}
{{- template "attribute" . }}
{{- end}}
    return nil
}
{{- end }}