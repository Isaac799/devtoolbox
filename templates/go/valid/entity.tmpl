{{- define "entity" }}
func ({{ renderCamel .Name }} *{{ renderPascal .Name }}) Valid() error {
    const label = "{{ .Name }}"
{{- range $index, $element := .AttributesToCreate }}
{{- template "attribute" . }}
{{- end}}
    return nil
}
{{- end }}