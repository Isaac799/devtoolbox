{{- block "default" . }}
{{- if .Attribute.HasDefault }} DEFAULT {{ renderDefault . }}{{ end }}
{{- end }}
{{- define "attribute" }}
{{- if .Attribute.HasErr }}
    {{- renderErrs . }}
{{- else }}
    {{- .Name }} {{ renderKind . }}{{ template "default" . }}
{{- end }}
{{- end }}